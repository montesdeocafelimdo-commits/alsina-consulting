/* ALSINA — flags de configuración global.
   PAYMENTS_ENABLED controla si el gate premium ofrece pago (Mercado
   Pago, Fase 6) o todavía captura mail como lista de espera. Cambiar
   este valor a `true` recién cuando Mercado Pago esté integrado y
   probado (ver README-PAGOS.md). */
window.ALSINA_CONFIG = {
  PAYMENTS_ENABLED: false,
};
